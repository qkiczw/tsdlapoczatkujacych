"use strict";
{
    const userName = "Lukasz";
    const userID = 6321;
    if (typeof userName === "string") {
        console.log("userName is a string.");
    }
    ;
    typeof userID === "number" ? console.log("User ID is a number") : console.log("User ID is not a number :/");
}
