"use strict";
const numberArr = [];
const stringArr = [];
numberArr.push(1);
numberArr.push(2);
// myArr.push("3")  // you cannot add a string to the array because it only accepts numbers
stringArr.push("Łukasz");
stringArr.push("Sztormowski");
// stringArr.push(40) // you cannot add a number to the array because it only accepts strings
console.log(numberArr);
console.log(stringArr);
